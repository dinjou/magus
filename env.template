# MAGUS Environment Configuration Template
# For production use, run: bash scripts/setup.sh
# This template is for CI/CD and testing purposes

# Django Configuration
DEBUG=False
SECRET_KEY=change-me-to-a-secure-random-50-character-string

# Domain Configuration
# For production, add your domain(s) here (comma-separated)
# CORS and CSRF origins are automatically built from ALLOWED_HOSTS
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (Internal)
POSTGRES_DB=magus_prod
POSTGRES_USER=magus_user
POSTGRES_PASSWORD=change-me-to-secure-password

# Redis Configuration (Internal)
REDIS_PASSWORD=change-me-to-secure-password

# Email Configuration (Optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@localhost

